# Expense Tracker API

API for Expense Tracker

## Tooling

This is a dotnet webapi template with a Postgres DB.

## Getting started

- Start Postgres inside docker with `docker compose up -d`.
- Migrate your db with `dotnet ef database update`.
- `dotnet run` starts the api server.
